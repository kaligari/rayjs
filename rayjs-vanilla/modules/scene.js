export default class Scene {
    constructor(camera, objects, lights, width, height) {
        this.camera = camera
        this.objects = objects
        this.lights = lights
        this.width = width
        this.height = height
    }
}
