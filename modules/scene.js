export default class Scene {
    constructor(camera, objects, width, height) {
        this.camera = camera
        this.objects = objects
        this.width = width
        this.height = height
    }
}
